
 #include <stdlib.h>
//#include <stdio.h>

#define MAX_LENGTH_BUFFER 256

int main(int argc, char** argv) {
  
  int SIG = 200
  //char **buffer

  do {
    
  }
  while (SIG == 0)
}

void read() {
  
}
void evaluate() {

}

void print() {

}

int parse(char **lexical, argv) {
  lexical.delimiters = char[] { "<", ">", "|" };
  lexical.reserved =  { "cat", "exit", "ls", "rm", /
			      "sort", "uniq", "wc"}
  lexical.type = char[] {"inputfile", "outputfile", "temp", "temp1", "NONE"}
}

int AST(char** argv) {
  
}
